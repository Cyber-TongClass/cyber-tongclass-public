# Synthetic Word fixtures

The detector/compiler/filler tests build minimal DOCX packages from the XML
fragments in this directory. Keeping the fixtures as text makes the structural
expectations reviewable while the tests still exercise real ZIP package bytes.

